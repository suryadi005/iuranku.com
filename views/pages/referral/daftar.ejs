<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../../partials/head'); %>
</head>
<body class="leading-normal tracking-normal text-white gradient" style="font-family: 'Source Sans Pro', sans-serif;">

<header>
    <%- include('../../partials/header'); %>
</header>

<main>
    <div class="pt-24">
        <% if (errorDaftarReferral) { %>
            <div class="container px-3 mx-auto flex flex-wrap flex-col md:flex-row items-center">
                <div class="w-full bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                    <span class="block sm:inline"><%= errorDaftarReferral %></span>
                    </span>
                </div>
            </div>
        <% } %>
        <div class="container px-3 mx-auto flex flex-wrap flex-col md:flex-row items-center">
            <!--Left Col-->
            <div class="w-full md:w-3/6 py-6 text-center">
                <div class="w-full md:w-4/5 z-50">
                    <%- include('../../partials/daftar-image') %>
                </div>
            </div>

            <!--Right Col-->
            <div class="flex flex-col w-full md:w-3/6 justify-center items-start text-left md:text-left">

                <div class="flex items-center min-h-screen dark:bg-gray-900" style="width: 100%;">
                    <div class="container mx-auto">
                        <div class="mx-auto my-10 bg-white p-5 rounded-md shadow-sm">
                            <div class="text-center">
                                <h1 class="my-3 text-3xl font-semibold text-gray-700 dark:text-gray-200">Daftar di program referral iuranku</h1>
                                <p class="text-gray-400 dark:text-gray-400">Silahkan isi form dibawah ini.</p>
                            </div>
                            <div class="m-7 text-gray-900">
                                <form action="/orders" method="POST" id="daftarForm">
                                    <div class="mb-6">
                                        <label for="code" class="block mb-2 text-sm text-gray-600 dark:text-gray-400">Custom code referral</label>
                                        <input type="text" name="code" id="code" placeholder="Biarkan kosong untuk mendapatkan code referral otomatis" class="w-full px-3 py-2 placeholder-gray-300 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-indigo-100 focus:border-indigo-300 dark:bg-gray-700 dark:text-white dark:placeholder-gray-500 dark:border-gray-600 dark:focus:ring-gray-900 dark:focus:border-gray-500" />
                                    </div>
                                </form>
                            </div>
                            <div class="mb-6">
                                <button id="tombolDaftar" type="submit" class="w-full px-3 py-4 text-white bg-indigo-500 rounded-md focus:bg-indigo-600 focus:outline-none">Daftar</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

    <%- include('../../partials/footer'); %>

<script>
    const daftarForm = document.getElementById('daftarForm')
    const tombolDaftar = document.getElementById('tombolDaftar')
    tombolDaftar.addEventListener('click', function() {
        if (daftarForm.reportValidity() === true) {
            tombolDaftar.setAttribute('disabled', true)
            daftarForm.submit()
        }
 
       
    })
</script>
</body>
</html>